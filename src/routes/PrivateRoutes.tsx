import { FC, memo } from "react";

/**
 * ==> props interface
 */
interface IProps {

}

/**
 * ==> Component
 */
const PrivateRoutes: FC<IProps> = ({  }) => {
  return (
    <>

    </>
  );
}

export default memo(PrivateRoutes);
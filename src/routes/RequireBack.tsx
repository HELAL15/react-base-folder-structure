import { FC, memo } from "react";

/**
 * ==> props interface
 */
interface IProps {

}

/**
 * ==> Component
 */
const RequireBack: FC<IProps> = ({  }) => {
  return (
    <>

    </>
  );
}

export default memo(RequireBack);
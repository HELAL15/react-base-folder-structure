import { FC, memo } from "react";

/**
 * ==> props interface
 */
interface IProps {

}

/**
 * ==> Component
 */
const Card: FC<IProps> = ({  }) => {
  return (
    <>

    </>
  );
}

export default memo(Card);